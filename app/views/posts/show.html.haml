%p#notice= notice

%h3= @post.title

%div.content
  = markdown(@post.body)

%br
- if current_user
  .form-actions
    = link_to 'Edit', edit_post_path(@post), class: 'btn'
    = link_to 'Back', posts_path, class: 'btn'
